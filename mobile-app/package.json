{
  "name": "khetha-path-mobile",
  "version": "1.1.0",
  "private": true,
  "description": "Khetha Path native mobile shell with local notifications",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "copy": "npx cap copy",
    "build": "npm run sync"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0",
    "@capacitor/push-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  }
}
