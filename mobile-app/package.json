{
  "name": "khetha-path-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Khetha Path mobile app wrapper",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "copy": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  }
}